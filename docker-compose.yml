services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vali_smart_agri_frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - VITE_API_BASE_URL=http://localhost:8000/api
